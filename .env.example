# SIEMBox Configuration
# Copy this file to .env and customize as needed

# ===== REQUIRED CONFIGURATION =====
# Database credentials
DB_PASSWORD=changeme

# Security keys - CHANGE THESE IN PRODUCTION!
# Generate with: openssl rand -hex 32
JWT_SECRET=change_this_in_production
ENCRYPTION_KEY=change_this_in_production

# ===== OPTIONAL API KEYS =====
# CrowdSec API key for threat intelligence
CROWDSEC_API_KEY=

# ===== PORT CONFIGURATION =====
# You can change these ports if they conflict with other services
# Frontend web interface
FRONTEND_PORT=3000

# API Gateway
API_PORT=8080

# Detection service
DETECTION_PORT=8001

# IP Lookup service
IPLOOKUP_PORT=8002

# VPS Audit service
VPS_AUDIT_PORT=8004

# Collector service HTTP API
COLLECTOR_PORT=8000

# Syslog ports (UDP and TCP)
SYSLOG_UDP_PORT=5514
SYSLOG_TCP_PORT=5514

# ===== ADVANCED CONFIGURATION =====
# Host address for external access (default: localhost)
# Change this if accessing from other machines
HOST_ADDRESS=localhost

# Database user (default: siembox)
DB_USER=siembox

# GitHub repository for pulling images
GITHUB_REPOSITORY=your-username/siembox