# SIEMBox Production Compose File
# Uses pre-built images from GitHub Container Registry (GHCR)
#
# Usage:
#   docker compose -f compose.prod.yaml up -d
#
# To use a specific version:
#   SIEMBOX_VERSION=1.0.0 docker compose -f compose.prod.yaml up -d

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: siembox-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-siembox}
      POSTGRES_USER: ${DB_USER:-siembox}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      # Loopback-only: reachable from this host, never the LAN. Needed so a
      # backend running with the optional network_mode: host below (which
      # drops it off siembox-network entirely) can still reach Postgres via
      # DB_HOST=127.0.0.1 (use the literal IP, not `localhost` -- this only
      # publishes the IPv4 loopback, and `localhost` can resolve to the IPv6
      # loopback first, which gets refused). Harmless left in place either way.
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-siembox}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - siembox-network
    restart: unless-stopped

  # Backend API and Syslog Server
  backend:
    image: ghcr.io/cladkins/siembox-backend:${SIEMBOX_VERSION:-latest}
    container_name: siembox-backend
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-8421}
      HOST: 0.0.0.0
      DB_HOST: ${DB_HOST:-postgres}
      DB_PORT: 5432
      # Set to true only alongside the optional network_mode: host below --
      # tells Log Discovery it can trust the host's real detected network
      # interface instead of the Docker bridge. See the comment on
      # network_mode below.
      SIEMBOX_HOST_NETWORKING: ${SIEMBOX_HOST_NETWORKING:-}
      DB_NAME: ${DB_NAME:-siembox}
      DB_USER: ${DB_USER:-siembox}
      DB_PASSWORD: ${DB_PASSWORD:-changeme}
      JWT_SECRET: ${JWT_SECRET:-change-this-secret-key}
      DEFAULT_ADMIN_PASSWORD: ${DEFAULT_ADMIN_PASSWORD:-changeme}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
      GEOIP_DB_PATH: ${GEOIP_DB_PATH:-/app/data/dbip-country-lite.mmdb}
      GEOIP_HOME_COUNTRIES: ${GEOIP_HOME_COUNTRIES:-}
      # Encrypts stored secrets (scanner creds + the AI API key). Generate with:
      #   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
      CREDENTIAL_ENCRYPTION_KEY: ${CREDENTIAL_ENCRYPTION_KEY:-}
      # AI builder (optional) — or paste the key in Settings -> AI Builder (encrypted at rest).
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      # Parser/detection catalog source (optional overrides; defaults to cladkins/siembox-parsers).
      SIEMBOX_CATALOG_REPO: ${SIEMBOX_CATALOG_REPO:-}
      SIEMBOX_CATALOG_REF: ${SIEMBOX_CATALOG_REF:-}
      GITHUB_TOKEN: ${GITHUB_TOKEN:-}
      # Catalog-only by default (no bundled parsers/detections seeded). Set to
      # true to opt back into auto-importing the bundled detection rules (legacy).
      SEED_BUNDLED_CONTENT: ${SEED_BUNDLED_CONTENT:-}
      # EDR YARA rule packs (optional). A small built-in starter bundle ships by
      # default and needs no config. Set EDR_YARA_FORGE_ENABLED=true to auto-import
      # the large YARA-Forge Extended pack daily (~16.6MB pulled per endpoint on
      # change). EDR_YARA_KEEP_VERSIONS caps stored bundle history (default 10).
      EDR_YARA_FORGE_ENABLED: ${EDR_YARA_FORGE_ENABLED:-}
      EDR_YARA_KEEP_VERSIONS: ${EDR_YARA_KEEP_VERSIONS:-}
    volumes:
      - ./rules:/app/rules:ro
      - ./data/geoip:/app/data
      - nuclei-templates:/root/nuclei-templates
      # Persist Trivy's vulnerability DB so container scans don't re-download it.
      - trivy-cache:/root/.cache/trivy
      # OPTIONAL — Docker host image discovery (Container Scanning → "Images on
      # this Docker host"). Uncomment to let the backend enumerate the images you
      # already run and scan them in one click. SIEMBox only issues read-only GETs,
      # but be aware: granting a container the Docker socket is effectively root on
      # the host (`:ro` does NOT make the Docker API read-only). Leave commented
      # out unless you accept that tradeoff; discovery degrades gracefully when absent.
      - /var/run/docker.sock:/var/run/docker.sock:ro
    # Lets the backend reach services on the Docker host (e.g. a local Ollama at
    # http://host.docker.internal:11434). Ollama must also bind 0.0.0.0.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      # Ignored entirely once network_mode: host is enabled below -- comment
      # this block out too when you do.
      - "8421:8421"
      - "514:514/udp"
      - "514:514/tcp"
    depends_on:
      postgres:
        condition: service_healthy
    # OPTIONAL -- Host networking for Log Discovery (Linux only). Passive
    # discovery (ARP table read, mDNS, SSDP) can only ever see the network(s)
    # this container is attached to -- on the bridge network below, that's
    # just the Docker bridge subnet, not your real LAN. Host networking gives
    # the backend the host's own network stack instead. Bigger tradeoff than
    # the socket mount above: the container is no longer network-isolated
    # from the host. See DEPLOYMENT.md's "Log Discovery and Network
    # Visibility" section before enabling. To enable: comment out `networks:`
    # below AND the `ports:` block above, uncomment the line below, then set
    # DB_HOST=127.0.0.1 (the literal IP, not `localhost` -- see the postgres
    # service's ports comment above), BACKEND_UPSTREAM=host.docker.internal:8421,
    # and SIEMBOX_HOST_NETWORKING=true in your .env file.
    # network_mode: host
    networks:
      - siembox-network
    restart: unless-stopped

  # Frontend (Vue.js + Nginx)
  frontend:
    image: ghcr.io/cladkins/siembox-frontend:${SIEMBOX_VERSION:-latest}
    container_name: siembox-frontend
    environment:
      # Where nginx proxies /api requests. Defaults to the backend's Docker-DNS
      # name on siembox-network. If you've enabled the backend's optional
      # network_mode: host above, set this to host.docker.internal:8421 in
      # your .env instead (see DEPLOYMENT.md's "Log Discovery and Network
      # Visibility" section).
      BACKEND_UPSTREAM: ${BACKEND_UPSTREAM:-backend:8421}
    ports:
      - "8420:80"
    depends_on:
      - backend
    networks:
      - siembox-network
    # Lets the frontend reach the backend on the Docker host when the backend
    # is running with the optional network_mode: host. Unused/harmless
    # otherwise -- same mechanism the backend already uses to reach a
    # host-local Ollama.
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  postgres-data:
    driver: local
  nuclei-templates:
    driver: local
  trivy-cache:
    driver: local

networks:
  siembox-network:
    driver: bridge
